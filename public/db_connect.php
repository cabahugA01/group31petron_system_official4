<?php
global $pdo;  $host = "localhost";
$dbname = "petron_pos_db_secure";
$user = "root";
$pass = ""; // XAMPP default is empty  try {  $pdo = new PDO(  "mysql:host=$host;dbname=$dbname;charset=utf8mb4",  $user,  $pass,  [PDO::ATTR_ERRMODE => PDO::ERRMODE_EXCEPTION]  );
} catch (PDOException $e) {  die("DB connection failed: " . $e->getMessage());
}  // ── Self-healing Database schema for pending_price_approvals ────────────────
try {  // Create table if it doesn't exist at all (full correct schema)  $pdo->exec("CREATE TABLE IF NOT EXISTS pending_price_approvals (  id INT AUTO_INCREMENT PRIMARY KEY,  station_id INT NOT NULL DEFAULT 1,  product_type VARCHAR(50) NOT NULL DEFAULT 'merchandise',  product_id INT NOT NULL DEFAULT 0,  product_name VARCHAR(255) DEFAULT NULL,  field_name VARCHAR(100) DEFAULT NULL,  old_cost DECIMAL(12,2) DEFAULT 0,  new_cost DECIMAL(12,2) DEFAULT 0,  old_price DECIMAL(12,2) DEFAULT 0,  new_price DECIMAL(12,2) DEFAULT 0,  old_value DECIMAL(12,2) DEFAULT 0,  new_value DECIMAL(12,2) DEFAULT 0,  manager_id INT DEFAULT NULL,  requested_by INT DEFAULT NULL,  admin_id INT DEFAULT NULL,  reviewed_by INT DEFAULT NULL,  reviewed_at DATETIME DEFAULT NULL,  status VARCHAR(30) NOT NULL DEFAULT 'pending',  rejection_reason TEXT DEFAULT NULL,  reviewer_notes TEXT DEFAULT NULL,  fuel_type_id INT DEFAULT NULL,  service_type_id INT DEFAULT NULL,  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,  INDEX idx_station (station_id),  INDEX idx_status (status),  INDEX idx_product (product_type, product_id)  ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4");  // Add any missing columns to existing table  $check_cols = $pdo->query("SHOW COLUMNS FROM pending_price_approvals");  $cols = $check_cols->fetchAll(PDO::FETCH_COLUMN);  $add_cols = [  'old_cost'  => "DECIMAL(12,2) DEFAULT 0",  'new_cost'  => "DECIMAL(12,2) DEFAULT 0",  'old_price'  => "DECIMAL(12,2) DEFAULT 0",  'new_price'  => "DECIMAL(12,2) DEFAULT 0",  'old_value'  => "DECIMAL(12,2) DEFAULT 0",  'new_value'  => "DECIMAL(12,2) DEFAULT 0",  'manager_id'  => "INT DEFAULT NULL",  'requested_by'  => "INT DEFAULT NULL",  'admin_id'  => "INT DEFAULT NULL",  'reviewed_by'  => "INT DEFAULT NULL",  'reviewed_at'  => "DATETIME DEFAULT NULL",  'rejection_reason' => "TEXT DEFAULT NULL",  'reviewer_notes'  => "TEXT DEFAULT NULL",  'fuel_type_id'  => "INT DEFAULT NULL",  'service_type_id'  => "INT DEFAULT NULL",  'updated_at'  => "DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP",  'product_name'  => "VARCHAR(255) DEFAULT NULL",  'field_name'  => "VARCHAR(100) DEFAULT NULL",  ];  foreach ($add_cols as $col => $def) {  if (!in_array($col, $cols)) {  try {  $pdo->exec("ALTER TABLE pending_price_approvals ADD COLUMN `$col` $def");  } catch (Exception $ae) { /* column might already exist */ }  }  }  // Drop foreign key constraints that would prevent inserting fuel or service products into pending_price_approvals  try {  $pdo->exec("ALTER TABLE pending_price_approvals DROP FOREIGN KEY fk_pending_price_approvals_product");  } catch (Exception $e) { /* doesn't exist or already dropped */ }  try {  $pdo->exec("ALTER TABLE pending_price_approvals DROP FOREIGN KEY fk_pending_price_approvals_product_id");  } catch (Exception $e) { /* doesn't exist or already dropped */ }  } catch (Exception $db_err) {  error_log("Db self-heal error: " . $db_err->getMessage());
}  