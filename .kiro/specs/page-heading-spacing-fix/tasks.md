# Implementation Plan

- [-] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - Zero Top Margin on .h1 Class
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the bug exists (pages using `.h1` class have 0px top margin)
  - **Scoped PBT Approach**: For this CSS bug, scope the property to concrete failing cases - all superadmin pages using `.h1` class
  - Test implementation: Verify that elements with `.h1` class currently have `margin-top: 0px` (from Bug Condition in design)
  - The test assertions should match the Expected Behavior: `.h1` elements should have `margin-top: 20px`
  - Create a test file (e.g., `tests/css/h1-spacing-test.html` or similar) that:
    - Loads `assets/css/style.css`
    - Creates elements with `.h1` class
    - Uses JavaScript to check computed style: `getComputedStyle(element).marginTop`
    - Asserts that `marginTop` should be "20px" (expected behavior)
  - Run test on UNFIXED code (current `assets/css/style.css` with `.h1{margin:0}`)
  - **EXPECTED OUTCOME**: Test FAILS with message like "Expected marginTop to be '20px', but got '0px'"
  - Document counterexamples found: "All `.h1` elements have 0px top margin instead of 20px, causing cramped appearance on superadmin pages"
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 1.1, 1.2, 1.3_

- [~] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Non-Top Margins and Non-.h1 Elements Unchanged
  - **IMPORTANT**: Follow observation-first methodology
  - Observe behavior on UNFIXED code for non-buggy cases (Preservation Requirements 3.1-3.5):
    - Elements with `.h1` class: `margin-right: 0px`, `margin-bottom: 0px`, `margin-left: 0px`
    - Elements with `.h1` class: `font-size: 28px`, `letter-spacing: -0.02em`
    - Pages without `.h1` class: maintain their current spacing
    - Other heading classes (`.page-head`, `.stock-page`, `.stock-title`): maintain their current styles
    - Manager/staff pages: maintain their current layout
  - Write property-based tests capturing observed behavior patterns:
    - Test 1: Verify `.h1` class has `margin-right: 0px` on unfixed code
    - Test 2: Verify `.h1` class has `margin-bottom: 0px` on unfixed code
    - Test 3: Verify `.h1` class has `margin-left: 0px` on unfixed code
    - Test 4: Verify `.h1` class has `font-size: 28px` on unfixed code
    - Test 5: Verify `.h1` class has `letter-spacing: -0.02em` on unfixed code
    - Test 6: Verify elements without `.h1` class are unaffected (e.g., regular headings)
    - Test 7: Verify other heading classes (`.page-head`, etc.) maintain their styles
  - Property-based testing approach: Test across different contexts (different pages, different elements)
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [~] 3. Fix for insufficient top margin on .h1 class

  - [ ] 3.1 Implement the fix in assets/css/style.css
    - Navigate to `assets/css/style.css` line 200
    - Change `.h1{margin:0;font-size:28px;letter-spacing:-.02em}` to:
    - `.h1{margin:20px 0 0 0;font-size:28px;letter-spacing:-.02em}`
    - This adds 20px top margin while keeping 0 margin on right, bottom, and left
    - Preserves all other properties (font-size, letter-spacing)
    - _Bug_Condition: Elements with `.h1` class on superadmin pages have `margin-top: 0px`_
    - _Expected_Behavior: Elements with `.h1` class should have `margin-top: 20px` for proper visual hierarchy_
    - _Preservation: Maintain `margin-right: 0px`, `margin-bottom: 0px`, `margin-left: 0px`, and all other CSS properties. Non-superadmin pages and pages without `.h1` class remain unchanged._
    - _Requirements: 1.1, 1.2, 1.3, 2.1, 2.2, 2.3, 3.1, 3.2, 3.3, 3.4, 3.5_

  - [ ] 3.2 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - 20px Top Margin on .h1 Class
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior (`.h1` should have `margin-top: 20px`)
    - When this test passes, it confirms the expected behavior is satisfied
    - Run bug condition exploration test from step 1
    - **EXPECTED OUTCOME**: Test PASSES with message like "✓ marginTop is '20px' as expected"
    - Confirms bug is fixed: superadmin pages now have proper top spacing
    - _Requirements: 2.1, 2.2, 2.3_

  - [ ] 3.3 Verify preservation tests still pass
    - **Property 2: Preservation** - Non-Top Margins and Other Elements Unchanged
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Verify:
      - `.h1` class still has `margin-right: 0px`
      - `.h1` class still has `margin-bottom: 0px`
      - `.h1` class still has `margin-left: 0px`
      - `.h1` class still has `font-size: 28px`
      - `.h1` class still has `letter-spacing: -0.02em`
      - Elements without `.h1` class are unaffected
      - Other heading classes maintain their styles
    - Confirm all preservation tests still pass after fix (no regressions)

- [~] 4. Checkpoint - Ensure all tests pass
  - Run all tests (bug condition + preservation) to verify complete fix
  - Manually verify on sample superadmin pages that spacing looks correct
  - Manually verify on manager/staff pages that spacing remains unchanged
  - Ask the user if questions arise or if manual verification reveals any issues
